The program is a single python file, which simply needs to be compiled and run with no arguments.

To set plaintext and initial key, change the 'p' and 'k' strings in the main function at the bottom of des.py

p = plaintext
k = initial key